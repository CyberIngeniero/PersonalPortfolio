export interface Service {
  icon: string;
  title: string;
  shortDesc: string;
  longDesc: string;
  features: string[];
}

export interface Education {
  year: string;
  title: string;
  organization: string;
  description: string;
}

export interface Experience {
  year: string;
  title: string;
  organization: string;
  description: string;
}